# xinci-simple 优化与现有样本回放

本次增加进入优势预检、独立投入建议、变现渠道依据和来源反馈调度。研究四态、资格 v2、搜索量准入及收入假设表保持不变；新候选标 workflow_version=3，完整核验前必须登记 ready 进入方案。

## 回放结果

2026-09-10 读取配置的数据区：14 个候选全部 rejected；13 个缺逐词量；17 份运行清单均为历史非结构化记录。将搜索量硬门改为仅排序，在这批已准入样本中新增 0 项。该样本受旧准入筛选，不能据此证明没有漏掉小量机会。

现有材料缺少低量对照样本及真实流量/收入反馈，无法据此决定放宽准入或调整 CTR、RPM、佣金。保留现行 50,000 簇量 / 5,000 主词量，以及 $200 模型月收入门槛。后续通过独立低量任务样本和实际反馈做比较，不用历史被否决项的旧模型收入冒充有效机会。逐候选数据见 [admission-replay.json](admission-replay.json)。

来源统计展示历史候选，但本次可归因学习样本为 0，因此仍采用默认轮换。新流程运行后，在样本足够时按可校验通过率和发现成本调整来源，保留每第四次发现的探索轮。

## 验证

125 项单元及 CLI 测试通过，覆盖新流程、历史读取、证据变化、重复计数、恢复来源、跳过发现与投入建议。实际账本 0 错误 / 0 警告；只读检查前后比较数据区全部文件 SHA-256，未改动业务数据。未进行新的 Semrush/Google 采集，未消耗查询额度。
